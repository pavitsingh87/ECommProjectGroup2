{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
            "scss:compile": "node-sass app.scss -o ./",
            "scss:watch": "npm run scss:compile -- --watch"
          
    },
    "devDependencies": {
        "axios": "^1.6.1",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
